server:
  port: 8081
spring:
  application:
    name: user-server
  cloud:
    #nacos配置
    nacos:
      server-addr: 127.0.0.1:8848
      #集群配置
      discovery:
        cluster-name: HZ
      config:
        file-extension: yml
  profiles:
    active: dev
#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:10086/eureka